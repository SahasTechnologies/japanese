{
  "name": "japanese",
  "version": "1.0.0",
  "type": "module",
  "description": "Japanese study app — JLPT N5 (Kana, Kanji, Vocab, Grammar, Reading, Listening) plus personal coursework units",
  "scripts": {
    "dev": "node scripts/fetch-kanjivg.js && node scripts/fetch-pitch.js && vite",
    "build": "node scripts/fetch-kanjivg.js && node scripts/fetch-pitch.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "wanakana": "^5.3.1"
  }
}
